const page='https://badmintonplayer.dk/DBF/Spiller/VisSpiller/#84737'; const html=await (await fetch(page)).text(); const m=html.match(/var SR_CallbackContext = '([^']+)'/); const out=[]; for(const getplayerdata of [true,false]) for(const showUserProfile of [true,false]) for(const showheader of [true,false]){const body={callbackcontextkey:m[1],seasonid:2025,playerid:84737,getplayerdata,showUserProfile,showheader}; const r=await fetch('https://badmintonplayer.dk/SportsResults/Components/WebService1.asmx/GetPlayerProfile',{method:'POST',headers:{'content-type':'application/json; charset=utf-8'},body:JSON.stringify(body)}); const j=await r.json(); const h=j.d?.Html||''; out.push({getplayerdata,showUserProfile,showheader,http:r.status,len:h.length,tournaments:/<h2>Turneringer<\/h2>/i.test(h),holdkampe:/<h2>Holdkampe<\/h2>/i.test(h),turneringLinks:(h.match(/\/DBF\/Turnering\/VisResultater/g)||[]).length});} console.log(JSON.stringify(out,null,2));
