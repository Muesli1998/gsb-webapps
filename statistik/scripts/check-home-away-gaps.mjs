import {DatabaseSync} from 'node:sqlite';import fs from 'node:fs';const d=new DatabaseSync('data/gsb-statistik-normalized.db');const by=d.prepare("select season_id, sum(case when home_name_raw is null or trim(home_name_raw)='' then 1 else 0 end) missing_home, sum(case when away_name_raw is null or trim(away_name_raw)='' then 1 else 0 end) missing_away, sum(case when result_raw is null or trim(result_raw)='' then 1 else 0 end) missing_result, count(*) total from team_matches group by season_id order by season_id").all();const examples=d.prepare("select external_match_id,season_id,gsb_team_id,status,home_name_raw,away_name_raw,result_raw from team_matches where (home_name_raw is null or trim(home_name_raw)='' or away_name_raw is null or trim(away_name_raw)='') limit 100").all();const report={generatedAt:new Date().toISOString(),bySeason:by,examples};fs.writeFileSync('results/home-away-gap-report.json',JSON.stringify(report,null,2));console.log({seasons:by.length,missingSides:examples.length});d.close();
