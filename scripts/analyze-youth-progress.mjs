import fs from 'node:fs';const files=fs.readdirSync('results/browser-fallback-youth').filter(x=>x.endsWith('.json'));const rows=files.map(f=>JSON.parse(fs.readFileSync('results/browser-fallback-youth/'+f)));const report={generatedAt:new Date().toISOString(),files:rows.length,statuses:{},withResult:0,withPlayers:0,walkover:0};for(const r of rows){report.statuses[r.status]=(report.statuses[r.status]||0)+1;const t=r.rawText||'';if(/^Resultat\s*\r?\n?\s*[^-\s]/m.test(t))report.withResult++;if(/\d+\.\s*[A-Z]/m.test(t))report.withPlayers++;if(/W\.O\.|Ikke fremmødt/i.test(t))report.walkover++}fs.writeFileSync('results/youth-progress-report.json',JSON.stringify(report,null,2));console.log(report)
