import fs from 'node:fs'; const p='results/browser-fallback-queue.json'; const q=JSON.parse(fs.readFileSync(p,'utf8')); const deferred=new Set(['2','3','4','5']); let n=0; for(const x of q.results){if(deferred.has(String(x.ageGroupId)) && x.status==='pending'){x.status='deferred_youth_u15';x.deferredReason='U15 og yngre udskudt efter prioriteringsbeslutning';n++;}} q.priorityNote='Aktuel fallback-prioritet: senior/veteran først; U15 og yngre udskudt.'; fs.writeFileSync(p,JSON.stringify(q,null,2)); console.log({deferred:n,pending:q.results.filter(x=>x.status==='pending').length});

