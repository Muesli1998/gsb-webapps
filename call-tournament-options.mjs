const qs=['query{tournamentGroups(seasonId:2025){id seasonId tierId groupName phaseType tournamentTier{id tierName}}}','query{tournamentTiers{id tierName}}']; for(const query of qs){const res=await fetch('https://app.nembadminton.dk/graphql',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query})});console.log(JSON.stringify(await res.json(),null,2));}
